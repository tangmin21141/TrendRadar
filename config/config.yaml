# 运行模式: cron
run_mode: "cron"

# 这里的定时设置对 GitHub Actions 不生效，它是靠 .github/workflows 里的设置控制的
# 但为了防止报错，保留默认即可
cron_schedule: "*/30 * * * *"
immediate_run: true

# 推送配置 (先关闭，我们先看网页版)
push:
  enable: false

# AI 配置 (先关闭)
llm:
  enable: false

filter:
  include_keywords: []
  exclude_keywords: []
